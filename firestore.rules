rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Leads collection - write-only from client, read restricted
    match /leads/{leadId} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}